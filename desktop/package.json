{
  "name": "switchman-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "CARGO_HOME=/private/tmp/switchman-cargo-home tauri dev",
    "dev:web": "vite --host 127.0.0.1 --port 1420",
    "build": "CARGO_HOME=/private/tmp/switchman-cargo-home tauri build",
    "build:web": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test:cli": "node --test tests/cli.test.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^7.0.0"
  }
}
